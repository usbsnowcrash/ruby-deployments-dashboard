.row
  .small-12.columns
    %h1
      Your Teams
- @teams.each do |team|
  .row.commit
    .small-12.large-5.columns
      = image_tag(team.organization.avatar_url, class: 'gravatar')
      = team.name
    .small-12.large-7.columns.commit-message
      = link_to 'Pull Requests',team_prs_path(teams: team.id)
      |
      = link_to 'Repositories',repos_path(team_id: team.id)
.row
  .small-12.columns
    %h1
      Other Repos
- @repos.each do |repo|
  .row.commit
    .small-12.large-5.columns
      = repo.full_name
    .small-12.large-7.columns.commit-message
      = link_to 'Deployment Log',deployments_path(user_name: repo[:html_url].gsub('https://github.com/','').split('/')[0] , repo_name: repo[:html_url].gsub('https://github.com/','').split('/')[1])