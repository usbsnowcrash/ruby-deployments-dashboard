.row
  .small-12.columns
    %h1 Production Versions
- @pulls.each do |pull|
  .row
    .small-12.large-6.columns
      = link_to pull["title"], main_app.version_path(:id => pull["number"].to_s)
    .small-12.large-6.columns
      =DateTime.parse(pull['merged_at']).strftime('%m/%d/%Y // %I:%M%p')