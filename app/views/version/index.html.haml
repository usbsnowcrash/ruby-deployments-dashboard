.row
  .small-12.columns
    %h1
      = "What was deployed on #{DateTime.parse(@thispull['merged_at']).strftime('%m/%d/%Y at %I:%M %p')}?"
.row
  .small-12.columns
    %h4
      Deployment title:
      %span.no-bold
        = @thispull['title']

.row
  .small-12.columns
    %h2 Changes to CB-Mobile
- @commits.each do |commit|
  .row.commit
    .small-3.columns
      - unless commit['author'].nil?
        = image_tag(commit['author']['avatar_url'], class: 'gravatar')
      - else
        = image_tag('https://i2.wp.com/a248.e.akamai.net/assets.github.com/images/gravatars/gravatar-user-420.png', class: 'gravatar')
      = commit['commit']['author']['name']
    .small-9.columns
      = link_to commit['commit']['message'], commit[:html_url]


.row
  .small-12.columns
    %h2 Changes to JobSearch
- @jobsearch_changes.each do |change|
  .row.commit
    .small-3.columns
      = image_tag(change[:user]['avatar_url'], class: 'gravatar')
      = change[:user]['login']
    .small-9.columns
      = link_to change[:title], change[:url]
- if @jobsearch_changes.count == 0
  .row
    .small-12.columns
      No changes
